<Window x:Class="ValheimWorldSync.Desktop.RecoveryWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Recuperação — Valheim World Sync" Width="820" Height="520" MinWidth="760" MinHeight="380" WindowStartupLocation="CenterOwner" Icon="vws.ico" Background="{StaticResource Background.Base}" Foreground="{StaticResource Text.Primary}" FontFamily="{StaticResource Font.Interface}" FontSize="13">
 <Grid Margin="24"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <StackPanel><TextBlock Text="Recuperação" Style="{StaticResource HeroTextStyle}"/><TextBlock Text="Cópias verificadas deste perfil" Foreground="{StaticResource Text.Muted}" Margin="0,4,0,0"/></StackPanel>
  <DataGrid Grid.Row="1" x:Name="EntriesGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,24,0,12" SelectionMode="Single" SelectionUnit="FullRow" GridLinesVisibility="None" HeadersVisibility="Column" BorderThickness="0" Background="{StaticResource Background.Base}" Foreground="{StaticResource Text.Primary}" RowBackground="{StaticResource Surface.Card}" AlternatingRowBackground="{StaticResource Background.Base}" RowHeight="36" HorizontalScrollBarVisibility="Auto">
   <DataGrid.Resources>
    <Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="{StaticResource Background.Base}"/><Setter Property="Foreground" Value="{StaticResource Text.Muted}"/><Setter Property="BorderBrush" Value="{StaticResource Border.Hairline}"/><Setter Property="BorderThickness" Value="0,0,0,1"/><Setter Property="Padding" Value="12,8"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
    <Style TargetType="DataGridCell"><Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="12,4"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="DataGridCell"><Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"><ContentPresenter VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{StaticResource Accent.Wash}"/><Setter Property="Foreground" Value="{StaticResource Text.Primary}"/></Trigger><Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter Property="Foreground" Value="{StaticResource Accent.Bronze}"/></Trigger></Style.Triggers></Style>
   </DataGrid.Resources>
   <DataGrid.Columns><DataGridTextColumn Header="Data" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM HH:mm}}" Width="160"/><DataGridTextColumn Header="Jogador" Binding="{Binding Player}" Width="150"/><DataGridTextColumn Header="Tamanho" Binding="{Binding Size}" Width="100"/><DataGridTextColumn Header="Origem" Binding="{Binding Origin}" Width="*" MinWidth="180"/></DataGrid.Columns>
  </DataGrid>
  <TextBlock Grid.Row="2" x:Name="ResultText" Foreground="{StaticResource Text.Muted}" TextWrapping="Wrap" AutomationProperties.LiveSetting="Polite"/>
  <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0"><Button Content="Guardar cópia…" Click="ExportClicked"/><Button Content="Restaurar neste computador" Click="RestoreClicked" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0"/><Button Content="Excluir" Click="DeleteClicked" Style="{StaticResource DestructiveButtonStyle}" Margin="24,0,0,0"/></StackPanel>
 </Grid>
</Window>
